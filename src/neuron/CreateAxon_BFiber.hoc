objref s[1]
create node[1]
objref fiber

proc CreateAxon_BFiber() {

    fiber = new bFiberBuilder(fiberD, axonnodes)

    axontotal = fiber.nsegments
    
    objref s[axontotal]

	for i = 0, fiber.nsegments-1 {
		s[i] = fiber.section[i]
	}


    v_init_b_fiber = fiber.v_init
}
